# =============================================================================
# Rust + Next.js Template Environment Configuration
# =============================================================================
# Copy this file to .env.local and update with your actual values:
#     cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# Rust Server (Main Entry Point)
# -----------------------------------------------------------------------------
# SERVER_PORT: Port where Rust server listens - main entry point (default: 3000)
# Users connect here for both /api and frontend
SERVER_PORT=3000

# SERVER_HOST: Host where Rust server binds (default: 127.0.0.1)
# For remote access: Set to 0.0.0.0
SERVER_HOST=127.0.0.1

# SERVER_PROXY_URL: URL to proxy non-/api requests to Next.js dev server (dev mode only)
# Default: http://localhost:${PORT}
# Production: Not used, Rust serves static files directly
# SERVER_PROXY_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Next.js Dev Server (Development Only)
# -----------------------------------------------------------------------------
# PORT: Port where Next.js dev server listens (default: 3001)
# Only used in development mode for hot reload
PORT=3001

# HOSTNAME: Host where Next.js dev server binds (default: localhost)
# For remote access: Set to 0.0.0.0
HOSTNAME=localhost

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# RUST_LOG: Rust logging level (default: info)
# Options: trace, debug, info, warn, error
RUST_LOG=info

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# DATABASE_URL: SQLite database file path
DATABASE_URL=sqlite:./rchat.db

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# SECRET_KEY: JWT secret key for authentication (REQUIRED for production)
# Generate a secure random key:
#   openssl rand -base64 32
SECRET_KEY=your-secret-key-change-this-in-production

# ADMIN_USERNAME: Optional default admin user to create on startup
# If set, this user will be created automatically with admin privileges
# Useful for development and testing
# ADMIN_USERNAME=admin
